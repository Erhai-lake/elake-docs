name: deploy blog to aliyun
on:
    push:
        branches:
        - master

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            # 使用 node:20
            - name: use Node.js 20
            uses: actions/setup-node@v1
            with:
                node-version: 20

            # 输出
            - name: s
                run: node -v
